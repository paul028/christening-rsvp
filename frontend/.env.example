# Public base URL for guest RSVP links copied from the admin dashboard.
# Set to your machine's LAN IP so links work on other devices on the same network.
# Leave unset to fall back to window.location.origin (localhost only).
VITE_PUBLIC_URL=http://192.168.x.x:5173
